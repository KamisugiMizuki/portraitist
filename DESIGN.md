# 个人性格画像深度分析产品 — 设计方案

> 状态：**Q1–Q6 已拍板（2026-08-11），待 UI fork 候选确认后进入 M1**
> 依据：IDEA.md + SYSTEM_PROMPT.md + 心理学研究调查（§2）
> 本阶段仅设计，不含代码实现。

## 0. 已拍板决策（用户决议，2026-08-11）

| 项 | 决议 |
|---|---|
| Q1 产品形态 | **本地 Web**：FastAPI 后端 + 浏览器前端；聊天 UI fork 成熟项目增量改 |
| Q2 LLM 后端 | **可切换**（远程 API 默认 / 本地 LM Studio 可选） |
| Q3 状态机归属 | **程序层控制**（§4.1 论证成立） |
| Q4 会话管理 | **多会话 + 历史报告留存** |
| Q5 危机边界 | **识别自伤信号 + 求助指引卡片**（M4 落地） |
| Q6 模型分级 | **对话轮轻量模型 / 报告强模型**（可配置） |

---

## 1. 产品来源

- **IDEA.md**：根据 SYSTEM_PROMPT.md 设计一款「对用户性格画像进行深入分析并提供分析报告」的产品。
- **SYSTEM_PROMPT.md**：定义了一位「心理探索引导师」的完整工作流——
  - 苏格拉底式提问，五层理论交叉采集（大五特质 / SDT 动机 / 罗杰斯自我认知 / 依恋与客体关系 / 麦克亚当斯叙事认同），每维度至少 2 个**具体生活场景行为锚点**；
  - 三阶段流程：询问期 → 终止条件判定（C-1 饱和度 / C-2 矛盾闭环 / C-3 信息增益下降 / C-4 深层反思触发，安全上限 12 轮）→ 画像确认请求 → 结构化报告；
  - 报告六段式：核心特质图谱（大五推论）、深层动力引擎、关系剧本与防御机制、生命叙事主线、综合真实自我描述、成长视角与盲点；
  - 强制非诊断声明，强调「陪伴者而非评判者」。

产品化的本质问题：**把这份 prompt 变成可控、可验证、可维护的系统**，而不是把整段 prompt 丢给 LLM 赌它长程遵守。

---

## 2. 心理学研究调查与对设计的启示

### 2.1 对话式 AI 人格评估的实证现状

| 研究 | 方法 | 关键发现 |
|---|---|---|
| *Can LLMs Assess Personality? Validating Conversational AI for Trait Profiling* (arXiv 2602.15848) | N=33 组内实验：引导式 LLM 对话 vs IPIP-50 金标准问卷 | 中等收敛效度 **r=0.38–0.58**；尽责性/开放性/神经质两法统计等价，**宜人性/外向性显著偏倚**（需特质特定校准）；用户感知准确度与问卷相当。→ 对话式评估「被用户接受」有实证，但**不同特质信度不均**，报告措辞需按特质区分置信度。 |
| *Patterns vs. Patients: Evaluating LLMs against MH Professionals on PD Diagnosis* (arXiv 2512.20298) | LLM vs 心理专家评估第一人称叙事 | 模型总分高于专家，但**对价值负载术语（"自恋"）严重漏诊**（F1 6.7 vs 50.0）；模型输出「自信、详尽、模式化」，专家「简洁、谨慎、关注自我感与时间性体验」。→ LLM 的自信详尽 ≠ 准确；**报告必须避免诊断式/标签式语言**，这正是 SYSTEM_PROMPT 的定位（促进觉察、非评判）。 |
| *Apparent Psychological Profiles of LLMs are Largely a Measurement Artifact* (arXiv 2606.20205) | 对 56 个 LLM 施测人格量表 + 人类对照 | 模型间差异 81–90% 来自**方向性响应偏差**而非特质（人类仅 9–16%）；「模型人格」可被题目选择制造。→ 虽然该研究测的是「LLM 当被试」而非「LLM 当访谈者」，但它给出核心警示：**不要相信 LLM 的「印象分」作为证据**。画像证据必须锚定用户自己的显式陈述（原话+具体事件），而不是模型的直觉打分。 |
| *LLM Psychometrics: A Systematic Review* (arXiv 2505.08245) | 综述 | LLM 心理测量学是新领域，静态基准已过时，需要**以人为中心的评估**。→ 本产品的验证不能靠「LLM 自评」，要靠用户侧反馈（画像确认、报告满意度）+ 结构完整性检查。 |

### 2.2 经典理论支撑（SYSTEM_PROMPT 的五层框架）

- **大五人格（Costa & McCrae; John & Srivastava）**：跨文化最稳健的人格结构。SYSTEM_PROMPT 的五条特质维度（能量/秩序/情绪敏感/开放/协作）与之对应。但注意：**量表测的是自陈倾向，行为锚点测的是具体情境表现**，两者互补——本产品走行为锚点路线（见 2.3）。
- **自我决定论（Deci & Ryan, 2017）**：自主/胜任/归属三大基本心理需求；动机内化程度决定行为质量。SYSTEM_PROMPT 的「主导驱动力」判定即此框架。
- **罗杰斯自我概念理论**：现实自我 vs 理想自我（及两者距离带来的感受）是自我觉察的核心入口，也是 12 轮内最容易触发 Aha Moment 的维度（C-4 的常见来源）。
- **成人依恋（Bowlby; Brennan et al. 1998 ECR）**：焦虑-回避两维空间描述成人依恋策略。「压力下第一反应（攻击/回避/讨好/理智化）」是依恋策略的行为指标——这是 SYSTEM_PROMPT 关系剧本层的操作化。
- **叙事认同（McAdams 生命故事访谈 LSI; Bauer, McAdams & Pals 2008）**：高潮/低潮/转折点的回忆组织出「核心人生剧本」，与幸福感（eudaimonic well-being）相关。「生命叙事主线」段即此。

### 2.3 测量学陷阱与对策

| 陷阱 | 含义 | 本产品对策 |
|---|---|---|
| **Barnum 效应**（Forer 1949） | 泛泛的人格描述被普遍接受为「准确」。 | 报告每条结论**强制引用用户原话/具体事件作为依据**；程序校验引用完整性，不达标则重生成。模糊描述与「人人皆准」句式列入黑名单。 |
| **社会赞许性偏差**（van de Mortel 2008） | 自陈偏向「好看」的方向。 | 提问设计减少价值评判（苏格拉底式、非评判语气）；报告区分「理想自我表达」与「实际行为锚点」，当两者冲突时标注为价值冲突点而非直接采信。 |
| **LLM 响应方向偏差**（arXiv 2606.20205） | 模型打分系统性偏向量表一端。 | 架构上**不让 LLM 输出量表分数**；画像证据 = 用户陈述的结构化提取，方向判定基于行为描述内容而非模型印象。 |
| **记忆回溯失真** | 自传式回忆不可靠。 | 锚点要求「具体场景 + 当时感受」，报告中将叙事素材标注为「主观回忆叙事」而非客观事实。 |

### 2.4 方法论文撑：为什么「行为锚点」是正确路线

- **Funder 现实准确性模型（RAM, 1995）**：人格判断准确性 = 相关线索可得 → 可得线索被察觉 → 被正确利用。结构化访谈通过**定向提问制造相关线索**（具体场景），比开放式闲聊线索密度高得多——这是 SYSTEM_PROMPT「每维度至少 2 个场景锚点」的科学依据。
- **关键事件技术（Flanagan CIT）**：用「回忆具体事件」替代「抽象自评」，是行为面试与胜任力评估的成熟方法，能显著降低社会赞许性与抽象化回答。

### 2.5 研究结论汇总（设计原则）

1. 对话式评估有效但**按特质置信度分级**——报告措辞需区分「高置信（锚点充分）」与「推测（锚点不足）」。
2. **证据锚定用户原话**，LLM 判断只是组织者，不是证据源。
3. **报告去标签化**：描述性语言 + 具体引用，避免诊断术语与 Barnum 式泛论。
4. 终止条件判定应**确定性、可验证**（程序层），不依赖 LLM 长程自律。
5. 产品定位诚实：**自我觉察工具**，非测评/诊断（非诊断声明照抄 SYSTEM_PROMPT 并加强）。

---

## 3. 产品定位

- **核心价值**：一次 8–12 轮的深度对话，产出一份「有证据、有深度、无标签」的个人心理画像报告，促进自我觉察。
- **目标用户**：本人（自用）+ 可分享给信任者（导出的报告）。单机个人工具定位。
- **边界（明确不做）**：
  - 不做临床诊断、不做职业测评/招聘筛选、不做量表分数（无 IPIP 施测）。
  - 不做危机干预——但**识别危机信号并提供求助指引**（§7.3）。
- **产品口号（暂定）**：让每个结论都有出处。

---

## 4. 总体架构

### 4.1 核心决策：状态机放程序层，LLM 只做三件事

**不采用**「整段 SYSTEM_PROMPT 丢给 LLM，靠模型内部 thinking 判断阶段」的单 prompt 方案。理由：

1. **终止条件必须确定性**：C-1（覆盖矩阵）、C-2（矛盾队列）、C-4（反思标记）、12 轮上限都是可计算的，程序判定 100% 可靠；LLM 长程判断 12 轮后漂移风险高（尤其本地 9B 级模型）。
2. **可测试性**：用户验收文化——程序层判定可写单测；LLM 判定无法回归验证。
3. **证据独立性**（§2.5-2）：锚点提取由程序调度，LLM 的响应偏差无法污染「哪些维度已饱和」这类元决策。
4. **成本**：每轮只发必要上下文（该维度已有锚点摘要 + 追问指令），而不是整段对话反复重发。

**LLM 的三个职责**：
1. **提问生成器**：给定「当前最欠饱和维度 + 已有锚点摘要 + 待澄清矛盾」，生成一条自然、非评判的苏格拉底式提问。
2. **回复提取器**：将用户回复结构化提取为锚点/叙事/矛盾信号（JSON 输出），并入证据库。
3. **报告生成器**：基于证据库（非原始对话）生成六段式报告草稿 → 程序校验引用 → 润色。

### 4.2 模块划分

```
┌─────────────────────────────────────────────┐
│  UI 层（形态待拍板 §9-Q1）                    │
│  CLI / 本地Web / Tauri 桌面                  │
└──────────────┬──────────────────────────────┘
               │ 对话事件
┌──────────────▼──────────────────────────────┐
│  对话引擎（程序状态机）                        │
│  · 阶段机：询问 → 判定 → 确认 → 报告          │
│  · 维度调度器：欠饱和维度优先 + 矛盾优先        │
│  · 追问触发器：无场景/过短回答 → 追问           │
│  · 终止判定器：C-1/C-2/C-3/C-4 + 12轮上限     │
└──────┬──────────────────────┬───────────────┘
       │ 指令                    │ 提取结果
┌──────▼────────┐      ┌────────▼──────────────┐
│ LLM 网关       │      │ 证据库（JSON，本地）    │
│ · 远程 API      │◄────►│ · 维度×锚点覆盖矩阵    │
│ · 本地 LM Studio│      │ · 矛盾队列+澄清记录    │
│ · 可切换        │      │ · 叙事事件表           │
└──────┬────────┘      │ · 反思/确认标记        │
       │                │ · 轮次日志             │
       │                └────────┬──────────────┘
       │                         │
┌──────▼─────────────────────────▼──────────────┐
│  报告生成器：证据→草稿→引用校验→六段式输出      │
│  导出：Markdown / HTML / PDF(后期)             │
└──────────────────────────────────────────────┘
```

### 4.3 LLM 网关

- 统一 OpenAI 兼容接口，配置项（`config.yaml`）：
  - `chat_model`: 对话轮轻量模型（低延迟、低成本），如 `deepseek-chat` / `qwen-turbo`；本地模式 `qwen3.5-9b@LM Studio`
  - `report_model`: 报告强模型（长文质量优先），如 `deepseek-reasoner` / `qwen-max` / `gpt-4o` 级
  - `backend`: `remote`（默认）/ `local`（LM Studio）；`base_url` / `api_key` 按模式配置
- **远程 API 模式**（默认，可分发性：clone 后填 key 即跑）；**本地 LM Studio 模式**（隐私优先，数据不出机）。
- 报告生成用高能力模型，对话轮次用低延迟模型——**模型分级已决议**（Q6）。本地模式无强模型时两档可指向同一模型。

---

## 5. 核心设计

### 5.1 维度体系与覆盖矩阵

| 层 | 维度 ID | 覆盖判定（每维 ≥2 锚点） |
|---|---|---|
| 特质层 | `trait_energy` 能量倾向（外倾/内倾） | 2 个含社交/独处场景的锚点 |
| | `trait_order` 秩序与随性 | 2 个涉及计划/拖延/环境的锚点 |
| | `trait_emotion` 情绪敏感性 | 2 个压力/情绪波动场景 |
| | `trait_openness` 认知开放性 | 2 个新事物/观点冲突场景 |
| | `trait_agree` 人际协作风格 | 2 个冲突/合作场景 |
| 动机层 | `motive_sdt` 主导驱力（自主/胜任/归属） | 2 个「为什么做某事」动机陈述 |
| | `motive_value` 自我提升 vs 自我超越 | 2 个价值排序场景 |
| 自我认知 | `self_discrepancy` 现实-理想自我距离 | 2 个自我评价/理想场景 |
| 关系模式 | `rel_attachment` 依恋策略 | 2 个亲密关系/分离场景 |
| | `rel_defense` 压力第一反应 | 2 个冲突即时反应场景 |
| 叙事认同 | `narrative_identity` 高潮/低潮/转折 | 至少 1 个叙事事件 + 1 个关键选择 |

共 11 个子维度。`narrative_identity` 的叙事素材同时服务于报告第 4 段。

### 5.2 证据库数据模型（JSON）

```jsonc
{
  "session_id": "…", "created_at": "…", "rounds": 12,
  "termination": { "c1": true, "c2": true, "c3": true, "c4": true, "c4_skipped": false,
                   "reached_12_round_cap": false, "notes": "…" },
  "dimensions": {
    "trait_energy": {
      "anchors": [
        { "quote": "用户原话片段", "scene": "具体场景摘要", "direction": "extravert|introvert|mixed",
          "round": 3, "confidence": "high|medium" }
      ],
      "saturated": true
    }
  },
  "contradictions": [
    { "a": {"quote": "…", "round": 2}, "b": {"quote": "…", "round": 5},
      "resolved": true, "resolution": "用户整合性解释（原话）" }
  ],
  "narratives": [
    { "type": "high|low|turning", "story": "…", "round": 7 }
  ],
  "reflection": { "triggered": true, "quote": "我从没这样想过…", "round": 6 },
  "confirmation": { "requested": true, "response": "认可|修正", "revision_notes": "…" }
}
```

- `direction` 由提取器基于**行为描述内容**判定（如「主动组织聚会」→ extravert），禁止 LLM 输出量表分。
- 原始对话全量另存 `sessions/<id>/transcript.jsonl`，报告可回溯。

### 5.3 对话引擎（状态机）

```
状态：COLLECTING → CLOSING_CHECK → CONFIRMING → REPORTING
              ↑_______________|                │
              └── 确认被否认 → 回到 COLLECTING（围绕分歧点）
```

每轮处理流水线：
1. **调度**：矛盾队列非空 → 优先澄清轮；否则选覆盖最欠饱和维度（随机化防顺序偏差）。
2. **提问生成**：LLM 输入 = 维度定义 + 已有锚点摘要（避免重复）+ **全局覆盖进度**（哪些维度已饱和、哪些缺，帮助后期集中火力）+ 语气约束（非评判、苏格拉底式、无「第 X 个问题」痕迹）；输出一条提问。
3. **回复处理**：LLM 提取器输入 = **上一轮提问文本**（关键：提取器需要知道用户在回答什么问题，才能按主题分配维度）+ 已有证据摘要 + 新回复；输出结构化 JSON（锚点/叙事/矛盾/反思/危机）；程序合并入证据库。每条回复可从多个维度提取锚点（每维度最多 1 个、总数最多 4 个），防单维度过载。
4. **追问判定**：无场景锚点/回答 < 阈值长度 → 生成追问（「可以分享一个具体的时刻吗？」），同一维度连续追问不超过 2 次。
5. **终止判定**（每轮后执行）：
   - C-1 覆盖矩阵全绿；
   - C-2 矛盾队列全 resolved 或为空；
   - C-3 最近 3 轮新增锚点数 ≤1 且无新矛盾/叙事/反思；
   - C-4 反思标记位（弹性：8 轮内未触发可跳过，报告注明）；
   - 上限：轮数 ≥12 强制进入阶段性画像流程（按 SYSTEM_PROMPT 安全上限语义）。
   - 满足 C-1+C-2+(C-3 或 C-4) 或 12 轮上限 → 进入 CONFIRMING。

**真实冒烟教训（2026-08-11 实测）**：
- 12 轮上限 vs 11 维度×2 锚点存在数学张力：每轮主题维度贡献 1 锚点，覆盖 22 锚点需要靠附带提取；实测典型会话 12 轮覆盖 8-10/11 维度，C-1 未全绿时按 SYSTEM_PROMPT 语义走"阶段性画像"（终止条件记录 reached_cap，报告正常生成）——这是设计内路径，不是异常。
- 提取器必须看到提问上下文，否则外向话痨的回复会被全部归入能量维度（实测 15/27 锚点集中在单维度）。
- 提取器维度标识容错链：英文 id > 中文名精确 > 关键词包含（"内向性"→trait_energy），丢弃锚点计数入 round_stats 供诊断。
- 报告标题格式三变体实测：`1. ` / `## 一、` / `**1. **` / `### 1.`——校验正则全部兼容；内部小节标题（"2. 价值冲突点"）不切段（只认六段主标题关键词）。
- **带反馈重试**是报告质量的关键机制：校验问题以人类可读文案（禁止正则原文）反馈给报告模型，实测 3 个 FAIL 会话 2-3 次内全部修复。
- 证据稀疏（锚点 <6）场景校验降级为 warning：阶段性画像允许证据有限，但黑名单（诊断词/Barnum）任何场景严格。
- Barnum 正则只拦"有时…有时/时而…时而"话术；"既…又…"是文学性报告的正常修辞，不拦（实测误伤「既孤独又圆满」）。
- LLM 网关对网络瞬断（SSL EOF/5xx）重试 3 次，4xx 不重试。
- DeepSeek v4 系为推理模型（reasoning_content 字段）：max_tokens 必须预留 reasoning 预算；OpenAI 兼容 `thinking: {type: enabled/disabled}` 可开关，提问/确认轮关闭保延迟、提取/报告轮开启保质量。

### 5.4 画像确认（CONFIRMING）

- 生成一段口语化核心轮廓摘要（LLM，输入=证据库摘要），作为确认请求发出。
- 用户认可/微调 → REPORTING；明确否认 → 记录分歧点，回 COLLECTING（分歧维度优先补采），补采后再次确认。
- 确认轮计入总轮数（受 12 轮上限约束：确认动作本身不强制增加轮数上限，但超限后仅允许一轮确认与一轮修订）。

### 5.5 报告生成（防 Barnum 设计）

生成流程：
1. **草稿**：LLM 输入 = 证据库（结构化）+ SYSTEM_PROMPT 报告模板（六段式 + 非诊断声明），输出 Markdown 草稿。
2. **引用校验（程序）**：检查每段是否包含 ≥1 条锚点引用（原话片段匹配或「依据轮次 N」标注）。**缺引用的段落标记为「推测」**并要求补引，或自动降级为「待补充探索」条目。
3. **标签检查（程序）**：黑名单词（诊断词：抑郁/焦虑障碍/自恋/偏执等 + Barnum 高频句式「你有时外向有时内向」类）→ 命中则打回重写。
4. **置信度标注**：按维度锚点数分档——3+ 锚点「依据充分」，2 锚点「初步」，1 锚点「推测」；C-4 未触发时在报告末尾注明「深层反思未充分触发」（SYSTEM_PROMPT 原文要求）。
5. 报告头部：非诊断声明（原文）+ 生成时间 + 对话轮数 + 分析依据说明。

报告结构 = SYSTEM_PROMPT 六段式原样（含「✨综合真实自我描述」文学性段落——由草稿段中最高质量素材组织）。

### 5.6 存储与隐私

- 本地目录 `sessions/<id>/`：`evidence.json` + `transcript.jsonl` + `report.md`。
- 远程 API 模式：界面明示「对话内容将发送至远程模型服务」；本地模式：数据不出机。
- 提供「删除会话」与「导出报告」；导出格式：Markdown（HTML/PDF 后期）。
- 不做账号体系、不做云同步（单机工具）。

---

## 6. UI 形态（已决议：本地 Web + fork 聊天 UI）

**决议**：本地 Web。FastAPI 后端 + 浏览器前端，聊天 UI fork 成熟项目增量改（候选评估见 §6.2）。

### 6.1 前后端架构与接口契约

```
浏览器（fork 的聊天 UI）
   │  HTTP/SSE
   ▼
FastAPI  (127.0.0.1:<port>)
   ├── /api/sessions          GET/POST  —— 会话列表 / 新建访谈会话
   ├── /api/sessions/{id}     GET/DELETE —— 会话详情 / 删除
   ├── /api/sessions/{id}/chat  POST     —— 发送一条用户消息，SSE 流式返回 AI 回复
   ├── /api/sessions/{id}/report GET     —— 已生成报告（Markdown + 元数据）
   ├── /api/sessions/{id}/status GET    —— 引擎内部状态（阶段/维度覆盖/轮数，供 UI 徽章）
   └── /api/config            GET        —— 模型配置回显（模型名、后端模式，只读）
   └── (可选) /v1/chat/completions      —— 若 fork 项目自带 API 层，走 OpenAI 兼容口
```

- **会话生命周期状态机（UI 可见）**：`active`（进行中）→ `confirming`（画像确认中）→ `completed`（报告已生成）→ `archived`（历史）。UI 会话列表按状态分组并显示徽章（进行中/待确认/已完成）。
- **聊天消息流**：用户消息 → POST /chat → 引擎内部跑一轮（调度→提问生成→回复提取→证据入库→终止判定），SSE 流式输出 AI 提问文本。回复提取与判定为引擎内部步骤，不暴露给 UI（SYSTEM_PROMPT「内部流程不告知用户」的要求由引擎层保证）。
- **报告视图**：报告不是普通聊天消息。`completed` 会话显示「查看报告」入口，报告页渲染六段式 Markdown + 维度置信度徽章 + 非诊断声明。导出 Markdown 按钮。
- **多会话管理**：会话列表（新建 / 恢复进行中会话 / 查看历史报告）。恢复进行中会话时引擎从证据库 + transcript 恢复状态（幂等）。
- 前端不做业务逻辑，全部走后端接口——保证 fork 项目的改动面最小（只改会话列表样式 + 加报告视图页）。

### 6.2 fork 候选评估（实测数据 2026-08-11，GitHub API + LICENSE 核实）

评估维度：架构复杂度（fork 后增量改的成本）、OpenAI 兼容支持、会话管理、Markdown/流式渲染、许可证、维护活跃度、本地部署形态。

| 候选 | 架构 | Stars / 许可证 / 活跃度 | 适配本项目的要点 | 风险 |
|---|---|---|---|---|
| **NextChat** (ChatGPTNext-Web) | Next.js + zustand，纯前端 + 可选内置 API 代理 | 88.6k / **MIT** / 2026-07 活跃 | 成熟多会话 + Markdown + 流式；可配 base_url 指向本地 FastAPI；单文件配置；**许可证最干净** | 新版 App Router 结构复杂，会话模型改动需动 store；本项目会话带生命周期状态，需增量开发 |
| Open WebUI | SvelteKit + FastAPI 后端 | 148k / **自定义 "Open WebUI License"（非 MIT，商用受限）** / 活跃 | LLM 后端 agnostic；自带后端，可扩展自定义管道 | 功能重（用户/权限/RAG 全家桶）；**许可证不干净**，fork 衍生受限 |
| LibreChat | Next.js + MongoDB | 41.9k / MIT / 活跃 | 功能全 | 需要 MongoDB，对单机工具过重 |
| LobeChat | Next.js | 81.5k / **LobeHub Community License（非 OSI 标准）** / 活跃 | 现代、插件生态 | 偏重；许可证非标准 |
| chatbot-ui (mckaywrigley) | Next.js | 33k / MIT / **2024-08 后停更** | 曾是轻量首选 | **已弃维护**，排除 |
| 自写极简聊天页（备选） | 原生 JS / React | — | 无 fork 成本，完全可控 | 违反「fork 成熟项目」偏好；聊天体验（流式、渲染）要自己打磨 |

**推荐：NextChat**。理由（实测）：
1. **MIT 许可证**——fork 改造成本与法律风险最低（Open WebUI / LobeChat 均为自定义受限许可证，排除）。
2. **纯前端架构**与「后端 = FastAPI」天然互补：NextChat 配 `base_url=http://127.0.0.1:<port>/v1` 即接管全部 LLM 通信，我们的 FastAPI 只需实现 OpenAI 兼容端点。
3. 多会话 / 流式 / Markdown / 移动端开箱即用；88k stars 的成熟度意味着 UI 细节无需打磨。
4. 维护活跃（2026-07 仍有推送）。

**NextChat fork 增量改点（克隆后确认）**：
- 会话列表：加生命周期状态分组与徽章（active/confirming/completed/archived），由 `/api/sessions/{id}/status` 驱动；
- 报告视图页：completed 会话的「查看报告」入口 + 六段式渲染 + 置信度徽章 + 导出按钮；
- 锁定/隐藏模型选择器与面具等杂项（本产品模型由后端配置决定，用户不可自选）；
- 会话持久化走后端 API（禁用 NextChat 自带 localStorage 存储，保证证据库单一事实源）。

### 6.3 NextChat 实测规模与瘦身方案（2026-08-11 clone 实测）

**实测**：单页应用（仅 1 个 `page.tsx`），约 41k 行 TS/TSX，36 个 dependencies，代码位于 `app/`（App Router 结构，无 monorepo）。store 为 10 个 zustand 切片。

**可用（核心资产，占大头）**：`store/chat.ts`（会话+消息+SSE 流式）、`store/access.ts`（API 配置）、`store/config.ts`（UI 配置）、聊天组件集（29 个）、`react-markdown` 渲染链、i18n 骨架、移动端适配、`fetch-event-source` 流式。

**删除清单（功能确实多，但都是独立模块，删得干净）**：

| 删除对象 | 说明 |
|---|---|
| `app/masks/` + `store/mask.ts` | 面具/角色预设（流程由引擎控制，不需要） |
| `app/mcp/` + `store/plugin.ts` | 插件与 MCP 生态（不需要） |
| `store/sd.ts` | SD 绘图集成 |
| `store/prompt.ts` | 快捷指令 |
| `store/sync.ts` | 云同步（本地单机） |
| `store/update.ts` | 更新检查 |
| `src-tauri/` | 桌面端（本项目是本地 Web） |
| 依赖 | mermaid、katex、emoji-picker-react、heic2any、html-to-image、MCP SDK、openapi-client-axios、spark-md5、fuse.js、@vercel/analytics、@vercel/speed-insights 等约半数 |

**决策：瘦身 fork**——把 NextChat 当一次性模板（**不追上游更新**），克隆后做一次"瘦身 commit"删除外围模块与依赖，保留聊天核心，再增量开发。删除成本低于自写聊天体验的成本；fork 不追上游则删除无合并冲突风险。轻量替代候选均已不可用（chatgpt-web 已 archived、chatbot-ui 停更、Open WebUI/LobeChat 许可证不干净），自写仅作最终备选。

---

## 7. 质量保障与伦理

### 7.1 验证方法

1. **状态机单测**：维度调度、终止判定器、追问触发器（确定性逻辑全覆盖）。
2. **模拟用户脚本**：用 LLM 扮演多种人格模板（外倾/内倾、高/低开放、矛盾型回答者、敷衍型回答者）跑完整对话，验证：阶段推进正确、12 轮上限触发、矛盾闭环、报告引用校验通过。**至少 5 种角色模板 × 每模板 2 次 = 10 次全流程模拟**。
3. **用户侧验证**：真实使用后，画像确认通过率 + 报告满意度（用户自评）。
4. **报告结构检查**：六段齐全、声明在位、引用校验 100% 通过、黑名单 0 命中。

### 7.2 报告质量的客观指标

- 引用覆盖率：每段 ≥1 锚点引用（100% 段落达标）。
- 轮次效率：典型会话 8–12 轮完成，非 12 轮强制截断。
- 矛盾闭环率：出现的矛盾 100% 有澄清记录。

### 7.3 危机边界（伦理底线，后置里程碑）

- 提取器输出 `crisis_signal` 字段；程序关键词层（自伤/自杀相关词汇）+ LLM 标注层双重识别。
- 触发后：停止追问该话题，回复共情 + 求助信息（本地心理援助热线/紧急渠道），报告中不加推测性分析。
- 定位声明：本产品不提供危机干预，仅提供求助指引。

---

## 8. 里程碑

| 里程碑 | 内容 | 验收标准 | 状态 |
|---|---|---|---|
| M0 | 设计确认 | 拍板问题全部决议 | ✅ 2026-08-11 |
| M1 | 核心引擎 CLI：状态机 + 证据库 + 提问/提取/报告三 LLM 调用 + 终止判定 | 模拟用户跑通全流程，终止条件判定单测通过 | ✅ 2026-08-11：42 单测 + 4 模板真实模拟（extravert/introvert/contradictory/terse），10 个 completed 会话报告校验全部 PASS |
| M2 | 报告质量打磨：引用校验、黑名单、置信度分级 | 引用覆盖率、黑名单 0 命中、六段齐全 | 🔄 基础已建（校验/黑名单/带反馈重试），深度打磨待 M2 |
| M3 | UI 形态落地（本地 Web + NextChat fork） | 真人完成 ≥1 次端到端访谈并导出报告 | ⏳ |
| M4 | 隐私/导出/危机边界 | 删除会话、导出 Markdown、危机卡片触发测试 | ⏳ |
| M5 | 多会话管理 + 报告对比（可选） | 两次访谈报告并列展示 | ⏳ |

---

## 9. 决策记录（Q1–Q6 已拍板，2026-08-11）

| 问题 | 决议 |
|---|---|
| Q1 产品形态 | 本地 Web：FastAPI + 浏览器，聊天 UI fork 成熟项目（候选见 §6.2，待确认） |
| Q2 LLM 后端 | 可切换，默认远程 API（可分发性），本地 LM Studio 可选 |
| Q3 状态机归属 | 程序层控制（§4.1） |
| Q4 会话管理 | 多会话 + 历史报告留存 |
| Q5 危机边界 | 识别自伤信号 + 求助指引卡片（M4） |
| Q6 模型分级 | 对话轮轻量 / 报告强模型，两档可配置 |

**下一步**：确认 §6.2 fork 候选 → M1 核心引擎（CLI 先行验证）。
